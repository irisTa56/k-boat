# Loopback URLs (if ever documented for local execution) are reachable only
# while a local process is running; lychee should not try to fetch them.
exclude = ["^http://127\\.0\\.0\\.1[:/]", "^http://localhost[:/]"]
